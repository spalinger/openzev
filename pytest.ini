[pytest]
DJANGO_SETTINGS_MODULE = config.settings
pythonpath = backend
python_files = tests.py test_*.py *_tests.py
addopts = -q
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests that exercise multiple components together
